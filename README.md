## 🚀 快速开始

# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产环境
pnpm build:pro

                   
## 🛠 技术栈选型

| 类别 | 技术 | 描述 |
| --- | --- | --- |
| **核心框架** | React 19 | 最新版 React，更高性能和更流畅的用户体验 |
| **路由管理** | React-Router v7 | 支持路由懒加载，优化页面切换性能 |
| **状态管理** | Zustand | 轻量级状态管理库，简单易用 |
| **样式方案** | SCSS + BEM | 模块化样式管理，结构清晰（可自行选择使用css module `xxx.module.scss`） |
| **HTTP库** | Axios | 统一处理 HTTP 请求和响应 |
| **工具库** | ahooks + es-toolkit | 丰富的 React Hooks 和 JS 工具函数 |
| **构建工具** | Vite 7 | 极速的构建工具，提升开发体验 |
| **类型检查** | TypeScript | 强大的类型系统，保障代码质量 |
| **代码规范** | ESLint + Prettier + Stylelint | 统一代码风格，提高代码质量 |



# 📦 React 移动端模板 - 最终文件结构

> 改造后的完整项目文件结构说明

---

## 📁 完整目录结构

```
react-mobile-ts-template/
│
├── .github/                      # GitHub配置
│   └── workflows/                # CI/CD配置（可选）
│
├── .husky/                       # Git Hooks
│   ├── pre-commit                # 提交前检查
│   └── commit-msg                # 提交信息检查
│
├── .vscode/                      # VSCode配置
│   ├── extensions.json           # 推荐插件
│   └── settings.json             # 编辑器设置
│
├── docs/                         # 📄 文档目录（新增）
│   ├── GETTING_STARTED.md        # 快速开始
│   ├── MOBILE_ADAPTION.md        # 移动端适配说明
│   ├── API_REQUEST.md            # API请求使用指南
│   ├── POPUP_MANAGE.md           # 弹窗管理指南
│   ├── STATE_MANAGE.md           # 状态管理指南
│   ├── HOOKS_GUIDE.md            # Hooks使用指南
│   └── DEPLOY.md                 # 部署指南
│
├── public/                       # 静态资源
│   ├── vite.svg                  # 图标
│   └── flexible.js               # 备用flexible方案（新增）
│
├── scripts/                      # 脚本目录
│   ├── push.sh                   # Git推送脚本
│   └── deploy.sh                 # 部署脚本（新增）
│
├── src/                          # 源代码目录
│   │
│   ├── api/                      # API接口定义
│   │   ├── index.ts              # 统一导出
│   │   ├── test.ts               # 测试接口
│   │   ├── user.ts               # 用户相关接口（新增）
│   │   └── common.ts             # 公共接口（新增）
│   │
│   ├── assets/                   # 资源文件
│   │   ├── images/               # 图片资源（新增）
│   │   ├── icons/                # SVG图标（新增）
│   │   │   ├── home.svg
│   │   │   ├── user.svg
│   │   │   └── setting.svg
│   │   └── react.svg
│   │
│   ├── components/               # 公共组件
│   │   ├── index.ts              # 统一导出
│   │   │
│   │   ├── auto-scroll-to-top/   # 自动滚动到顶部
│   │   │   └── index.tsx
│   │   │
│   │   ├── loading/              # 加载组件
│   │   │   ├── index.tsx
│   │   │   └── index.scss
│   │   │
│   │   ├── not-found/            # 404页面
│   │   │   └── index.tsx
│   │   │
│   │   ├── safe-area/            # 🆕 安全区域组件
│   │   │   ├── index.tsx
│   │   │   └── index.scss
│   │   │
│   │   ├── nav-bar/              # 🆕 导航栏组件
│   │   │   ├── index.tsx
│   │   │   └── index.scss
│   │   │
│   │   ├── tab-bar/              # 🆕 底部导航组件
│   │   │   ├── index.tsx
│   │   │   └── index.scss
│   │   │
│   │   ├── pull-refresh/         # 🆕 下拉刷新组件
│   │   │   ├── index.tsx
│   │   │   └── index.scss
│   │   │
│   │   └── popups/               # 🆕 弹窗组件集合
│   │       ├── index.ts
│   │       ├── demo-popup.tsx    # 示例弹窗
│   │       └── confirm-popup.tsx # 确认弹窗
│   │
│   ├── constants/                # 📌 常量定义（新增）
│   │   ├── index.ts              # 统一导出
│   │   ├── popup-names.ts        # 弹窗名称枚举
│   │   ├── storage-keys.ts       # 存储键名枚举
│   │   └── common.ts             # 公共常量
│   │
│   ├── hooks/                    # 自定义Hooks
│   │   ├── index.ts              # 统一导出
│   │   │
│   │   ├── use-router/           # 路由操作Hook
│   │   │   └── index.ts
│   │   │
│   │   ├── use-namespace/        # BEM命名Hook
│   │   │   └── index.ts
│   │   │
│   │   ├── use-ref-state/        # ref+state Hook
│   │   │   └── index.ts
│   │   │
│   │   ├── create-design/        # 设计模式Hook
│   │   │   └── index.ts
│   │   │
│   │   ├── use-popup/            # 🆕 弹窗管理Hook
│   │   │   ├── index.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── use-touch/            # 🆕 触摸手势Hook
│   │   │   ├── index.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── use-scroll/           # 🆕 滚动监听Hook
│   │   │   ├── index.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── use-viewport/         # 🆕 视口信息Hook
│   │   │   ├── index.ts
│   │   │   └── types.ts
│   │   │
│   │   ├── use-safe-area/        # 🆕 安全区域Hook
│   │   │   └── index.ts
│   │   │
│   │   └── use-network/          # 🆕 网络状态Hook
│   │       ├── index.ts
│   │       └── types.ts
│   │
│   ├── router/                   # 路由配置
│   │   ├── index.tsx             # 路由主文件
│   │   │
│   │   ├── modules/              # 路由模块
│   │   │   └── test.tsx
│   │   │
│   │   └── utils/                # 路由工具
│   │       ├── index.ts
│   │       ├── lazy-load.tsx     # 懒加载
│   │       ├── route-guard.tsx   # 路由守卫
│   │       ├── page-title.tsx    # 🆕 页面标题
│   │       └── route-transition.tsx # 🆕 页面动画（可选）
│   │
│   ├── services/                 # 🔧 服务层（增强）
│   │   ├── index.ts              # 统一导出
│   │   ├── service.ts            # axios实例配置
│   │   │
│   │   ├── interceptors/         # 🆕 拦截器目录
│   │   │   ├── index.ts
│   │   │   ├── request.ts        # 请求拦截器
│   │   │   └── response.ts       # 响应拦截器
│   │   │
│   │   ├── cancel.ts             # 🆕 请求取消控制
│   │   ├── error-handler.ts      # 🆕 错误处理
│   │   └── types.ts              # 🆕 类型定义
│   │
│   ├── store/                    # 状态管理（优化）
│   │   ├── index.ts              # 统一导出
│   │   ├── create-store.ts       # 🆕 Store创建工具
│   │   ├── types.ts              # 🆕 类型定义
│   │   │
│   │   └── modules/              # Store模块
│   │       ├── use-user-store.ts      # 用户状态
│   │       ├── use-app-store.ts       # 🆕 应用全局状态
│   │       ├── use-loading-store.ts   # Loading状态
│   │       └── use-popup-store.ts     # 🆕 弹窗状态
│   │
│   ├── styles/                   # 样式管理（增强）
│   │   │
│   │   ├── scss/                 # SCSS样式
│   │   │   ├── index.scss        # 总入口
│   │   │   ├── var.scss          # 变量
│   │   │   ├── mixins.scss       # 🆕 Mixins
│   │   │   ├── global.scss       # 全局样式
│   │   │   ├── reset.scss        # ✏️ 重置样式（移动端优化）
│   │   │   ├── antd-mobile-custom.scss # 🆕 UI库主题定制
│   │   │   └── tools.scss        # 样式工具函数
│   │   │
│   │   └── css/                  # CSS样式
│   │       └── reset.css         # 备用重置样式
│   │
│   ├── types/                    # 类型定义
│   │   ├── global.d.ts           # 全局类型
│   │   ├── router.d.ts           # 路由类型
│   │   ├── api.d.ts              # 🆕 API类型
│   │   └── vite-env.d.ts         # Vite环境类型
│   │
│   ├── utils/                    # 工具函数（增强）
│   │   ├── index.ts              # 统一导出
│   │   ├── tools.ts              # 通用工具
│   │   ├── dateUtil.ts           # 日期工具
│   │   ├── file.ts               # 文件工具
│   │   ├── mobile.ts             # 🆕 移动端工具
│   │   ├── storage.ts            # 🆕 存储工具
│   │   ├── url.ts                # 🆕 URL工具
│   │   ├── validate.ts           # 🆕 验证工具
│   │   └── flexible.ts           # 🆕 Flexible方案
│   │
│   ├── views/                    # 页面视图
│   │   │
│   │   ├── home/                 # ✏️ 首页（重写）
│   │   │   ├── index.tsx
│   │   │   └── index.scss
│   │   │
│   │   ├── examples/             # 🆕 示例页面
│   │   │   │
│   │   │   ├── list/             # 列表示例
│   │   │   │   ├── index.tsx
│   │   │   │   └── index.scss
│   │   │   │
│   │   │   ├── detail/           # 详情示例
│   │   │   │   ├── index.tsx
│   │   │   │   └── index.scss
│   │   │   │
│   │   │   ├── form/             # 表单示例
│   │   │   │   ├── index.tsx
│   │   │   │   └── index.scss
│   │   │   │
│   │   │   ├── popup-demo/       # 弹窗示例
│   │   │   │   ├── index.tsx
│   │   │   │   └── index.scss
│   │   │   │
│   │   │   └── hooks-demo/       # Hooks示例
│   │   │       ├── index.tsx
│   │   │       └── index.scss
│   │   │
│   │   └── test/                 # 测试页面
│   │       ├── login/
│   │       ├── count/
│   │       └── auth-test/
│   │
│   ├── app.tsx                   # 根组件
│   ├── error-boundary.tsx        # 错误边界
│   └── main.tsx                  # ✏️ 入口文件（引入flexible）
│
├── .commitlintrc.js              # Commitlint配置
├── .editorconfig                 # EditorConfig配置
├── .env.dev                      # ✏️ 开发环境变量
├── .env.test                     # ✏️ 测试环境变量
├── .env.pro                      # ✏️ 生产环境变量
├── .eslintrc.js                  # ESLint配置（已废弃）
├── .gitignore                    # Git忽略文件
├── .npmrc                        # npm配置
├── .prettierignore               # Prettier忽略文件
├── .prettierrc.js                # Prettier配置
├── .stylelintignore              # Stylelint忽略文件
├── eslint.config.js              # ESLint配置（新版Flat Config）
├── index.html                    # ✏️ HTML模板（添加viewport）
├── LICENSE                       # 许可证
├── lint-staged.config.js         # lint-staged配置
├── package.json                  # ✏️ 项目配置（更新依赖）
├── pnpm-lock.yaml                # 依赖锁文件
├── postcss.config.ts             # 🆕 PostCSS配置
├── README.md                     # ✏️ 项目说明（重写）
├── README.en-US.md               # ✏️ 英文说明（重写）
├── REFACTOR_PLAN.md              # 改造计划文档
├── REFACTOR_FILE_STRUCTURE.md    # 本文档
├── stylelint.config.js           # Stylelint配置
├── tailwind.config.js            # ✏️ Tailwind配置（移动端优化）
├── tsconfig.json                 # TypeScript配置
└── vite.config.ts                # ✏️ Vite配置（移动端优化）

```

---

## 📝 目录说明

### 新增目录

#### 1. `docs/` - 文档目录
存放所有项目文档，包括快速开始、使用指南、部署说明等。

#### 2. `src/constants/` - 常量定义
集中管理项目中的所有常量、枚举等。

#### 3. `src/hooks/use-popup/` - 弹窗管理Hook
完整的弹窗管理解决方案。

#### 4. `src/hooks/use-touch/` 等移动端Hooks
5个移动端专用Hooks，提供触摸、滚动、视口等功能。

#### 5. `src/services/interceptors/` - 拦截器目录
请求和响应拦截器的独立管理。

#### 6. `src/components/safe-area/` 等移动端组件
移动端专用的UI组件。

#### 7. `src/views/examples/` - 示例页面
提供完整的功能示例和最佳实践。

---


## 📦 核心文件说明

### API层 (`src/api/`)
```typescript
// 示例结构
export const getUserInfo = (id: string) => {
  return get<UserInfo>('/user/info', { id })
}
```

### Services层 (`src/services/`)
```
service.ts         - axios实例和基础配置
interceptors/      - 拦截器逻辑
  ├── request.ts   - 请求拦截（添加token等）
  └── response.ts  - 响应拦截（错误处理等）
cancel.ts          - 请求取消控制
error-handler.ts   - 统一错误处理
types.ts           - 类型定义
```

### Store层 (`src/store/`)
```
create-store.ts    - Store创建工具（支持持久化）
modules/
  ├── use-user-store.ts    - 用户信息、登录状态
  ├── use-app-store.ts     - 应用全局状态（主题等）
  ├── use-loading-store.ts - 全局Loading
  └── use-popup-store.ts   - 弹窗状态管理
```

### Hooks层 (`src/hooks/`)
```
基础Hooks（保留）：
  ├── use-router/       - 路由操作
  ├── use-namespace/    - BEM命名
  ├── use-ref-state/    - ref+state
  └── create-design/    - 设计模式

移动端Hooks（新增）：
  ├── use-popup/        - 弹窗管理
  ├── use-touch/        - 触摸手势
  ├── use-scroll/       - 滚动监听
  ├── use-viewport/     - 视口信息
  ├── use-safe-area/    - 安全区域
  └── use-network/      - 网络状态
```

### Utils层 (`src/utils/`)
```
通用工具：
  ├── tools.ts      - 通用工具函数
  ├── dateUtil.ts   - 日期处理
  └── file.ts       - 文件处理

移动端工具（新增）：
  ├── mobile.ts     - 设备判断、环境检测
  ├── storage.ts    - 增强版localStorage/sessionStorage
  ├── url.ts        - URL参数处理
  ├── validate.ts   - 常用验证（手机号、身份证等）
  └── flexible.ts   - Flexible适配方案
```

### Components层 (`src/components/`)
```
通用组件（保留）：
  ├── loading/           - 加载组件
  ├── not-found/         - 404页面
  └── auto-scroll-to-top/ - 自动滚动

移动端组件（新增）：
  ├── safe-area/         - 安全区域
  ├── nav-bar/           - 导航栏
  ├── tab-bar/           - 底部导航
  ├── pull-refresh/      - 下拉刷新
  └── popups/            - 弹窗集合
```

### Views层 (`src/views/`)
```
业务页面：
  ├── home/              - 首页

示例页面（新增）：
  └── examples/
      ├── list/          - 列表示例（下拉刷新、上拉加载）
      ├── detail/        - 详情示例
      ├── form/          - 表单示例
      ├── popup-demo/    - 弹窗示例
      └── hooks-demo/    - Hooks示例
```
---

## 🎨 样式架构

### SCSS文件组织
```scss
styles/scss/
├── index.scss              // 总入口，导入所有样式
├── var.scss                // 变量定义（颜色、字号等）
├── mixins.scss             // Mixins（常用样式函数）
├── global.scss             // 全局样式
├── reset.scss              // 重置样式（移动端优化）
├── antd-mobile-custom.scss // UI库主题定制
└── tools.scss              // 样式工具类
```

### 样式使用方式
```typescript
// 1. 全局样式（已自动引入）
// 2. 模块化SCSS
import styles from './index.module.scss'

// 3. Tailwind类名
<div className="flex items-center px-4">

// 4. 混合使用
<div className={`${styles.container} flex`}>
```

---

## 🔧 配置文件说明

### `postcss.config.ts`
```typescript
// PostCSS配置，主要是postcss-pxtorem
export default {
  plugins: {
    'postcss-pxtorem': {
      rootValue: 75,
      propList: ['*'],
      selectorBlackList: ['norem', 'ant-'],
      exclude: /node_modules\/(?!antd-mobile)/,
    },
    autoprefixer: {},
  },
}
```

### `tailwind.config.js`
```javascript
// 移动端断点配置
module.exports = {
  theme: {
    screens: {
      'xs': '320px',
      'sm': '375px',
      'md': '414px',
      'lg': '768px',
    },
  },
}
```

### `.env.dev` / `.env.test` / `.env.pro`
```bash
# 环境标识
VITE_NODE_ENV=development

# API配置
VITE_API_BASE_URL=/api
VITE_SERVER_URL=https://dev-api.example.com

# 移动端配置（新增）
VITE_ENABLE_VCONSOLE=true
VITE_ENABLE_FLEXIBLE=true
VITE_DESIGN_WIDTH=750
VITE_ROOT_VALUE=75

# 端口配置
VITE_APP_PORT=3000

# 输出目录
VITE_OUT_DIR=dist
```

---

## 📱 移动端特性文件

### 1. Flexible适配
```
src/utils/flexible.ts     - 主实现
public/flexible.js        - 备用方案
```

### 2. 移动端Hooks
```
src/hooks/
├── use-touch/       - 手势操作
├── use-scroll/      - 滚动处理
├── use-viewport/    - 视口信息
├── use-safe-area/   - 安全区域
└── use-network/     - 网络状态
```

### 3. 移动端工具
```
src/utils/mobile.ts  - 设备判断、环境检测
```

### 4. 移动端组件
```
src/components/
├── safe-area/       - 安全区域组件
├── nav-bar/         - 导航栏
├── tab-bar/         - 底部导航
└── pull-refresh/    - 下拉刷新
```

---

## 📚 文档文件说明

### `docs/COMPONENTS_GUIDE.md`
移动端适配说明，包括：
- px-to-rem原理
- flexible使用
- Tailwind响应式
- 注意事项

### `docs/API_REQUEST_USAGE.md`
API请求使用指南，包括：
- 基础用法
- 拦截器说明
- 错误处理
- 类型定义

### `docs/POPUP_MANAGE_USAGE.md`
弹窗管理指南，包括：
- 使用方式
- API说明
- 最佳实践
- 示例代码

### `docs/STATE_MANAGE_USAGE.md`
状态管理指南，包括：
- Zustand基础
- Store创建
- 持久化配置
- 最佳实践

### `docs/HOOKS_GUIDE.md`
Hooks使用指南，包括：
- 所有Hooks说明
- 使用示例
- 注意事项

### `docs/UTILS_GUIDE.md`
工具函数使用指南


### 🚀 高性能构建

- 基于 Vite 7 构建，冷启动时间快至毫秒级
- 支持代码分割和动态导入，优化首屏加载速度

### 🛡️ 完善的类型系统

- 完整的 TypeScript 类型定义
- 严格的 tsconfig 配置，开启所有严格检查选项
- 统一的类型管理，便于维护和协作

### 🎨 规范化的代码风格

- 集成 ESLint、Prettier、Stylelint 三大代码规范工具
- 统一的 commit message 规范（Commitlint + Husky）
- 自动格式化代码，保证团队代码风格一致性

### 🔧 强大的开发工具链

- react-dev-inspector 集成，点击页面元素直接跳转到源码
- import 顺序自动排序，提高代码可读性
- 多环境配置（dev/test/pro），满足不同部署需求

## 📦 关于路由缓存 keep-alive

> React 官方暂时没有实现 vue \<keep-alive\> 类似的功能。React 官方出于两点考虑拒绝添加这个功能，具体可以自行搜索查阅。为了达到状态保存的效果，官方推荐以下两种手动保存状态的方式：

- 将需要保存状态组件的 state 提升至父组件中保存。
- 使用 CSS visible 属性来控制需要保存状态组件的渲染，而不是使用 if/else，以避免 React 将其卸载。

> 不过也有一些相关库实现了这个功能，如：`react-router-cache-route、react-activation、keepalive-for-react` 等等，如果项目中需要状态缓存处理的数据量较小，那最好还是按照 React 官方的建议，手动解决状态缓存问题。

## ⚠️ 注意事项

> 1. 目前有一些 ui 库或其他第三方库还尚未支持 `react19`，注意甄别安装使用。
> 2. 本项目并未使用 19 版本的相关特性，如需要可以直接使用如下命令降级到 18 版本。

```bash
pnpm install react@18.3.1 react-dom@18.3.1
```